# SwapImageFrames

選択した画像入りフレームを、リンク画像だけ／フレーム位置ごとのいずれかの方法で順送りに入れ替えます。

## 主な機能

- 「画像リンクを入れ替え」は既存画像を削除し、別フレームのリンク画像を順送りに再配置
- 「フレームごと入れ替え」は中身を保ったままフレームの位置だけを入れ替え
- 配置後のフィット方法（塗り／全体表示）と、位置の基準（左上／中央）を選択
- 入れ替え順は見た目の位置で「上から下、左から右」

## 使い方

1. 画像入りフレームを 2 つ以上選択する
2. スクリプトを実行する
3. 入れ替えモードとオプションを指定して［OK］

## 制限事項・メモ

- 同じフレームが重複選択されていても 1 回だけ処理します。
- 「画像リンクを入れ替え」は各フレームに主画像が 1 点だけある前提です。
- 削除・再配置・フィットに失敗した場合は処理を中止してエラーを表示します。

## スクリプト情報

| 項目 | 内容 |
| --- | --- |
| ファイル | `jsx/SwapImageFrames.jsx` |
| バージョン | v1.0 |
| 作者 | Masahiro Takano (@swwwitch) |
| 初回リリース | 2026-03-28 |
| 最終更新 | 2026-03-28 |

## ライセンス

MIT License — <http://opensource.org/licenses/mit-license.php>
